<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #f2f2f2;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
      }
      .home-container {
        background: white;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.2);
        width: 300px;
        text-align: center;
      }
      h1 {
        margin-bottom: 20px;
      }
      p {
        font-size: 18px;
        margin: 10px 0;
      }
      .logout-button {
        margin-top: 20px;
        padding: 10px;
        background-color: #f44336;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 16px;
      }
      .logout-button:hover {
        background-color: #e53935;
      }
    </style>
  </head>
  <body>
    <div class="home-container">
      <h1>Welcome, <%= userInfo.name%></h1>
      <p>Email: <%= userInfo.email%></p>
      <button class="logout-button" onclick="window.location.href='/logout'">
        Log Out
      </button>
      <button class="logout-button" onclick="window.location.href='/logout'" style="background-color: rgb(6, 179, 84);">
        <a href="todo" style="text-decoration: none; color: #ffffff;">ToDo items</a>
      </button>
    </div>
  </body>
</html>
